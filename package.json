{
  "scripts": {
    "test": "lerna run --parallel test",
    "build": "lerna run --parallel build",
    "clean": "lerna run --parallel clean",
    "rebuild": "$npm_execpath run clean  &&  $npm_execpath run build",
    "prepublish": "$npm_execpath run rebuild  &&  $npm_execpath test"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/DaAitch/froq"
  },
  "keywords": [
    "test",
    "orchestration",
    "integration"
  ],
  "author": "Philipp Renoth <renoth@wildoak.de>",
  "license": "MIT",
  "description": "Integration Testing environment orchestration made easy.",
  "devDependencies": {
    "lerna": "^2.7.0"
  }
}
